# =============================================================================
# Inventaire Production - Multi Serveurs (2 VPS)
# =============================================================================
---
all:
  children:
    openclaw_servers:
      hosts:
        vps-openclaw-01:
          ansible_host: "173.212.208.181"
          ansible_port: "{{ ssh_port }}"
          ansible_user: deploy
          ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
          # Variables spécifiques au serveur 1
          openclaw_subdomain: "portal-7x2k"
        vps-openclaw-02:
          ansible_host: "176.57.184.246"
          ansible_port: "{{ ssh_port }}"
          ansible_user: deploy
          ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
          # Variables spécifiques au serveur 2 (subdomain différent si même domaine)
          openclaw_subdomain: "portal-9y4m"
      vars:
        env: production
