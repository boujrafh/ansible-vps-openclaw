# =============================================================================
# Inventaire Staging
# =============================================================================
---
all:
  hosts:
    vps-staging:
      ansible_host: "{{ vault_staging_ip | default(vault_vps_ip) }}"
      ansible_port: "{{ ssh_port }}"
      ansible_user: deploy
      ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
  vars:
    env: staging
